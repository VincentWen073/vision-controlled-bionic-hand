# requirements.txt

# Core libraries for computer vision and numerical operations
numpy
# Install the full version of OpenCV, which includes main modules and contrib modules.
# Do not install opencv-python alongside this.
opencv-contrib-python

# MediaPipe for hand tracking
mediapipe

# Raspberry Pi specific library (will be ignored on non-Pi systems)
picamera2

# Serial communication library
pyserial